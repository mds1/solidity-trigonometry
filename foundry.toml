[profile.default]
src = 'src'
out = 'out'
libs = ['lib']

# test config
ffi = true
fuzz-runs = 100
gas_reports = ["*"]
verbosity = 3

# solidity config
solc-version = "0.8.13"
optimizer = true
optimizer-runs = 9999999

[profile.ci]
fuzz-runs = 50000
